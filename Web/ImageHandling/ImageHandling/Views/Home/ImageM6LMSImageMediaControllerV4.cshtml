@{
    ViewData["Title"] = "Serverside Controller V4 android hack + Script + Cookie - Image Large Medium Small";
}

<script>
     document.addEventListener("DOMContentLoaded", () => {
         function setCookie(name, value, days = 7) {
             const expires = new Date(Date.now() + days * 864e5).toUTCString();
             document.cookie = name + '=' + encodeURIComponent(value) + '; expires=' + expires + '; path=/';
         }

         function getDeviceType() {
             const width = window.innerWidth;
             if (width < 768) return "mobile";
             if (width < 1024) return "tablet";
             return "desktop";
         }

         function updateCookie() {
             const deviceType = getDeviceType();
             setCookie("deviceType", deviceType, 7);
         }

        updateCookie();
     });
</script>

<div class="container mt-5">
	<h2>Server Controller V4 android hack + Script Cookie - Large Medium Small</h2>

	Needs to be called 2 times
	<img src="/responsive-imageV4/image.jpeg" />

</div>